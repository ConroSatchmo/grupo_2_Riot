<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head', {nameStyle: 'login' , titleHTML: 'Login' })
    %>
  </head>

  <body>
    <%- include('./partials/header') %>

    <main>
      <section class="login">
        <div class="mensaje">
          <h2>¡Bienvenido! Ingresá tu email y contraseña.</h2>
          <% if(locals.msg){ %>
            <p><%= msg %></p>
            <% } %>
        </div>
        <form
          action="/users/login"
          method="post"
          class="create-form"
          onsubmit="validateForm(event)"
          name="myForm"
        >
          <div class="user">
            <label for="email">Email</label>
            <input type="text" name="email" required />
          </div>
          <div class="contrasenia">
            <label for="password">Contraseña</label>
            <input
              type="password"
              name="password"
              id="password"
              required
            />
          </div>
          <div class="olvidaste"><a href="#">¿Olvidaste tu contraseña?</a></div>
          <div class="botones">
            <button type="submit" class="boton">Ingresar</button>
            <a href="/users/register" class="crea">Creá una cuenta</a>
          </div>
        </form>
      </section>
    </main>

    <%- include('./partials/footer') %>

    <script src="/scripts/login.js"></script>
  </body>
</html>
